// APP
export const HIERARCHY_ROOT_PATH = '/authorized/cases/path';

// VISUALIZATIONS
export const DEFAULT_BG_CONFIG = [
  {
    dataReference: 0,
    lossless: false,
    protocol: '`{"type":"leav3","slide":"${data}"}`',
  },
];

export const DEFAULT_PARAMS_CONFIG = {
  locale: 'en',
  activeBackgroundIndex: 0,
  activeVisualizationIndex: 0,
};
